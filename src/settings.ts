// filepath: /Volumes/pi-share-homebridge-plugins/homebridge-aldesVMC/src/settings.ts
/**
 * This is the name of the platform that users will use to register the plugin in the Homebridge config.json
 */
export const PLATFORM_NAME = 'AldesVMC';

/**
 * This must match the name of your plugin as defined the package.json
 */
export const PLUGIN_NAME = 'homebridge-aldesVMC'; // Make sure this matches package.json name